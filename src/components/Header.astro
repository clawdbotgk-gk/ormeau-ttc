<!-- Navigation -->
<nav id="main-header" class="sticky top-0 z-[100] bg-[#1a1a1a]/95 backdrop-blur-md border-b border-gray-800 header-nav transition-transform duration-300" aria-label="Main navigation">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-10">
    <div class="flex justify-between items-center px-4 md:px-6 lg:px-10 py-3 md:py-0 h-[60px] md:h-20">
      <div class="flex-shrink-0 flex items-center group cursor-pointer gap-4">
        <a href="/" class="relative overflow-hidden rounded-xl shadow-sm transition-transform duration-300 group-hover:scale-105">
          <img alt="Ormeau Logo" class="h-8 md:h-10 lg:h-14 w-auto" src="https://xkwg7luliofrc1cs.public.blob.vercel-storage.com/Images/logoOrmeauInverse%402x.png" loading="eager"/>
        </a>
      </div>
      <div class="hidden md:flex flex-1 justify-center space-x-12">
        <a class="text-gray-300 hover:text-primary font-medium text-lg transition-all duration-200 nav-link" href="/">Home</a>
        <a class="text-gray-300 hover:text-primary font-medium text-lg transition-all duration-200 nav-link" href="/about">About</a>
        <div class="relative group">
          <button type="button" class="text-gray-300 hover:text-primary font-medium text-lg transition-all duration-200 cursor-pointer flex items-center gap-1" aria-expanded="false" aria-haspopup="true" aria-label="Club menu">
            Club <span class="material-icons text-sm" aria-hidden="true">expand_more</span>
          </button>
          <div class="absolute left-0 top-full pt-2 hidden group-hover:block group-focus-within:block z-50">
            <div class="bg-gray-800 rounded-lg shadow-xl py-2 min-w-40">
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/timetable">Timetable</a>
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/events">Events</a>
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/teams">Teams</a>
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/champions">Champions</a>
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/partners">Partners</a>
            </div>
          </div>
        </div>
        <a class="text-gray-300 hover:text-primary font-medium text-lg transition-all duration-200 nav-link" href="/contact">Contact</a>
      </div>
      <div class="hidden md:flex items-center gap-4">
        <a class="px-8 py-3.5 rounded-full bg-blue-600 text-white font-bold hover:bg-blue-700 transition-all duration-300 shadow-lg shadow-blue-600/25 hover:shadow-blue-600/40 transform hover:-translate-y-0.5 active:translate-y-0" href="https://ormeautt.com/" target="_blank" rel="noopener noreferrer">Club App</a>
        <a class="px-8 py-3.5 rounded-full bg-primary text-white font-bold hover:bg-primary-hover transition-all duration-300 shadow-lg shadow-primary/25 hover:shadow-primary/40 transform hover:-translate-y-0.5 active:translate-y-0" href="/join">Join Now</a>
      </div>
      <div class="md:hidden flex items-center">
        <button id="mobile-menu-btn" class="flex items-center justify-center text-gray-300 hover:text-primary focus:outline-none p-2 min-w-[44px] min-h-[44px]" aria-label="Toggle menu" aria-expanded="false">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>
</nav>

<script is:inline>
  (function() {
    const path = window.location.pathname;
    document.querySelectorAll('.nav-link').forEach(link => {
      const href = link.getAttribute('href');
      if ((path === '/' && href === '/') || href === path || href === path + '/') {
        link.classList.add('text-primary', 'dark:text-primary');
      }
    });
  })();
</script>

<script is:inline>
  // Header scroll shadow effect
  (function() {
    const header = document.querySelector('.header-nav');
    if (!header) return;
    
    let lastScroll = 0;
    window.addEventListener('scroll', function() {
      const currentScroll = window.pageYOffset || document.documentElement.scrollTop;
      if (currentScroll > 10) {
        header.classList.add('header-scrolled');
      } else {
        header.classList.remove('header-scrolled');
      }
      lastScroll = currentScroll;
    }, { passive: true });
  })();
</script>

<!-- Mobile Menu Panel -->
<div id="mobile-menu" class="fixed inset-0 z-50 md:hidden hidden">
  <!-- Backdrop -->
  <div id="mobile-menu-backdrop" class="absolute inset-0 bg-black/60 backdrop-blur-sm"></div>
  
  <!-- Menu Panel -->
  <div class="absolute right-0 top-0 w-[300px] max-w-[85vw] h-full bg-gradient-to-b from-[#1f1f1f] to-[#171717] shadow-2xl flex flex-col" id="mobile-menu-panel">
    <!-- Header with logo and close -->
    <div class="flex-shrink-0 flex items-center justify-between p-5 border-b border-white/5">
      <a href="/" class="flex-shrink-0">
        <img alt="Ormeau Logo" class="h-10 w-auto" src="https://xkwg7luliofrc1cs.public.blob.vercel-storage.com/Images/logoOrmeauInverse%402x.png" loading="eager"/>
      </a>
      <button id="mobile-menu-close" type="button" onclick="document.getElementById('mobile-menu').classList.add('hidden'); document.body.style.overflow = ''; document.getElementById('main-header').style.transform = 'translateY(0)';" class="w-12 h-12 flex items-center justify-center rounded-full bg-white/10 text-gray-300 hover:text-white hover:bg-white/20 transition-all relative z-[60] cursor-pointer">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 pointer-events-none" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
    
    <!-- Navigation Links - scrollable -->
    <nav class="flex-1 overflow-y-auto px-4 py-6">
      <a href="/" class="mobile-nav-item menu-item">
        <span class="text-lg font-medium text-gray-200">Home</span>
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
        </svg>
      </a>
      <a href="/about" class="mobile-nav-item menu-item">
        <span class="text-lg font-medium text-gray-200">About</span>
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
        </svg>
      </a>
      
      <!-- Club Dropdown (always visible for simplicity) -->
      <div class="menu-item">
        <div class="text-xs uppercase tracking-wider text-gray-500 px-3 py-2 mt-2 mb-1">Club</div>
        <div class="flex flex-col bg-white/3 rounded-xl overflow-hidden">
          <a href="/timetable" class="mobile-nav-subitem">Timetable</a>
          <a href="/events" class="mobile-nav-subitem">Events</a>
          <a href="/teams" class="mobile-nav-subitem">Teams</a>
          <a href="/champions" class="mobile-nav-subitem">Champions</a>
          <a href="/partners" class="mobile-nav-subitem">Partners</a>
        </div>
      </div>
      
      <a href="/contact" class="mobile-nav-item menu-item">
        <span class="text-lg font-medium text-gray-200">Contact</span>
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
        </svg>
      </a>
    </nav>
    
    <!-- Action Buttons - fixed at bottom -->
    <div class="flex-shrink-0 p-5 bg-gradient-to-t from-[#171717] via-[#1a1a1a] to-transparent border-t border-white/5">
      <div class="flex flex-col gap-3">
        <a href="https://ormeautt.com/" target="_blank" rel="noopener noreferrer" class="flex items-center justify-center gap-2 w-full py-3.5 bg-blue-600 hover:bg-blue-500 text-white font-semibold rounded-xl transition-all active:scale-[0.98]">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 21v-8.25M15.75 21v-8.25M8.25 21v-8.25M3 9l9-6 9 6m-1.5 12V10.332A48.36 48.36 0 0012 9.75c-2.551 0-5.056.2-7.5.582V21M3 21h18M12 6.75h.008v.008H12V6.75z" />
          </svg>
          Club App
        </a>
        <a href="/join" class="flex items-center justify-center gap-2 w-full py-3.5 bg-primary hover:bg-primary-hover text-white font-semibold rounded-xl transition-all active:scale-[0.98]">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
          </svg>
          Join Now
        </a>
      </div>
    </div>
  </div>
</div>

<script is:inline>
  // Mobile menu toggle - simplified for reliability
  document.addEventListener('DOMContentLoaded', function() {
    const menuBtn = document.getElementById('mobile-menu-btn');
    const closeBtn = document.getElementById('mobile-menu-close');
    const mobileMenu = document.getElementById('mobile-menu');
    const mobileMenuPanel = document.getElementById('mobile-menu-panel');
    const backdrop = document.getElementById('mobile-menu-backdrop');
    const mainHeader = document.getElementById('main-header');
    
    // Open mobile menu
    function openMobileMenu() {
      if (mobileMenu) mobileMenu.classList.remove('hidden');
      document.body.style.overflow = 'hidden';
      // Delay header hiding slightly so sidebar opens first
      setTimeout(function() {
        if (mainHeader) {
          mainHeader.style.transform = 'translateY(-100%)';
        }
      }, 50);
    }
    
    // Close mobile menu
    function closeMobileMenu() {
      if (mobileMenu) mobileMenu.classList.add('hidden');
      document.body.style.overflow = '';
      if (mainHeader) {
        mainHeader.style.transform = 'translateY(0)';
      }
    }
    
    // Menu button click
    if (menuBtn) {
      menuBtn.addEventListener('click', function(e) {
        e.preventDefault();
        openMobileMenu();
      });
    }
    
    // Close button click
    if (closeBtn) {
      closeBtn.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        closeMobileMenu();
      });
    }
    
    // Backdrop click
    if (backdrop) {
      backdrop.addEventListener('click', function(e) {
        e.preventDefault();
        closeMobileMenu();
      });
    }
    
    // Escape key handler
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && !mobileMenu.classList.contains('hidden')) {
        closeMobileMenu();
      }
    });
    
    // Close menu when clicking on navigation links
    const navLinks = document.querySelectorAll('#mobile-menu a');
    navLinks.forEach(link => {
      link.addEventListener('click', function() {
        closeMobileMenu();
      });
    });
  });
</script>
