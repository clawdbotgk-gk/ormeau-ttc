<!-- Navigation -->
<nav class="sticky top-0 z-50 bg-[#1a1a1a]/95 backdrop-blur-md border-b border-gray-800" aria-label="Main navigation">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-10">
    <div class="flex justify-between items-center h-20 sm:h-28">
      <div class="flex-shrink-0 flex items-center group cursor-pointer gap-4">
        <a href="/" class="relative overflow-hidden rounded-xl shadow-sm transition-transform duration-300 group-hover:scale-105">
          <img alt="Ormeau Logo" class="h-10 sm:h-14 w-auto" src="https://www.ormeautabletennis.com/wp-content/uploads/2021/12/logoOrmeauInverse@2x-e1508851762736.png" loading="eager"/>
        </a>
      </div>
      <div class="hidden md:flex flex-1 justify-center space-x-12">
        <a class="text-gray-300 hover:text-primary font-medium text-lg transition-all duration-200 nav-link" href="/">Home</a>
        <a class="text-gray-300 hover:text-primary font-medium text-lg transition-all duration-200 nav-link" href="/about">About</a>
        <div class="relative group">
          <button type="button" class="text-gray-300 hover:text-primary font-medium text-lg transition-all duration-200 cursor-pointer flex items-center gap-1" aria-expanded="false" aria-haspopup="true" aria-label="Club menu">
            Club <span class="material-icons text-sm" aria-hidden="true">expand_more</span>
          </button>
          <div class="absolute left-0 top-full pt-2 hidden group-hover:block group-focus-within:block z-50">
            <div class="bg-gray-800 rounded-lg shadow-xl py-2 min-w-40">
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/timetable">Timetable</a>
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/events">Events</a>
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/teams">Teams</a>
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/champions">Champions</a>
              <a class="block px-4 py-2 text-gray-200 hover:bg-primary/10 hover:text-primary" href="/partners">Partners</a>
            </div>
          </div>
        </div>
        <a class="text-gray-300 hover:text-primary font-medium text-lg transition-all duration-200 nav-link" href="/contact">Contact</a>
      </div>
      <div class="hidden md:flex items-center gap-4">
        <a class="px-8 py-3.5 rounded-full bg-blue-600 text-white font-bold hover:bg-blue-700 transition-all duration-300 shadow-lg shadow-blue-600/25 hover:shadow-blue-600/40 transform hover:-translate-y-0.5 active:translate-y-0" href="https://ormeautt.com/" target="_blank" rel="noopener noreferrer">Club App</a>
        <a class="px-8 py-3.5 rounded-full bg-primary text-white font-bold hover:bg-primary-hover transition-all duration-300 shadow-lg shadow-primary/25 hover:shadow-primary/40 transform hover:-translate-y-0.5 active:translate-y-0" href="/join">Join Now</a>
      </div>
      <div class="md:hidden flex items-center">
        <button id="mobile-menu-btn" class="flex items-center text-gray-300 hover:text-primary focus:outline-none p-2">
          <span class="material-icons text-3xl" aria-hidden="true">menu</span>
        </button>
      </div>
    </div>
  </div>
</nav>

<script is:inline>
  (function() {
    const path = window.location.pathname;
    document.querySelectorAll('.nav-link').forEach(link => {
      const href = link.getAttribute('href');
      if ((path === '/' && href === '/') || href === path || href === path + '/') {
        link.classList.add('text-primary', 'dark:text-primary');
      }
    });
  })();
</script>

<!-- Mobile Menu Panel -->
<div id="mobile-menu" class="fixed inset-0 z-40 md:hidden hidden">
  <!-- Backdrop -->
  <div id="mobile-menu-backdrop" class="absolute inset-0 bg-black/50 backdrop-blur-sm"></div>
  
  <!-- Menu Panel -->
  <div class="absolute right-0 top-0 h-full w-80 max-w-full bg-[#1a1a1a] shadow-xl transform translate-x-full transition-transform duration-300" id="mobile-menu-panel">
    <div class="flex flex-col h-full pt-6 px-6">
      <div class="flex justify-between items-center mb-8">
        <a href="/" class="flex-shrink-0">
          <img alt="Ormeau Logo" class="h-12 w-auto" src="https://www.ormeautabletennis.com/wp-content/uploads/2021/12/logoOrmeauInverse@2x-e1508851762736.png" loading="eager"/>
        </a>
        <button id="mobile-menu-close" class="p-2 text-gray-300 hover:text-primary transition-colors">
          <span class="material-icons text-3xl" aria-hidden="true">close</span>
        </button>
      </div>
      <nav class="flex flex-col space-y-6 text-xl">
        <a href="/" class="text-gray-300 font-semibold hover:text-primary transition-colors mobile-nav-link">Home</a>
        <a href="/about" class="text-gray-300 font-semibold hover:text-primary transition-colors mobile-nav-link">About</a>
        <div class="space-y-4">
          <div class="font-semibold text-gray-300">Club</div>
          <div class="ml-4 space-y-3">
            <a href="/timetable" class="block text-gray-400 hover:text-primary transition-colors mobile-nav-link">Timetable</a>
            <a href="/events" class="block text-gray-400 hover:text-primary transition-colors mobile-nav-link">Events</a>
            <a href="/teams" class="block text-gray-400 hover:text-primary transition-colors mobile-nav-link">Teams</a>
            <a href="/champions" class="block text-gray-400 hover:text-primary transition-colors mobile-nav-link">Champions</a>
            <a href="/partners" class="block text-gray-400 hover:text-primary transition-colors mobile-nav-link">Partners</a>
          </div>
        </div>
        <a href="/contact" class="text-gray-300 font-semibold hover:text-primary transition-colors mobile-nav-link">Contact</a>
        <div class="flex flex-col gap-3 mt-4">
          <a href="https://ormeautt.com/" target="_blank" rel="noopener noreferrer" class="px-6 py-3 bg-blue-600 text-white font-bold text-center rounded-full hover:bg-blue-700 transition-colors">Club App</a>
          <a href="/join" class="px-6 py-3 bg-primary text-white font-bold text-center rounded-full hover:bg-primary-hover transition-colors">Join Now</a>
        </div>
      </nav>
    </div>
  </div>
</div>

<script is:inline>
  // Mobile menu toggle
  (function() {
    const menuBtn = document.getElementById('mobile-menu-btn');
    const closeBtn = document.getElementById('mobile-menu-close');
    const mobileMenu = document.getElementById('mobile-menu');
    const mobileMenuPanel = document.getElementById('mobile-menu-panel');
    const backdrop = document.getElementById('mobile-menu-backdrop');
    
    // Open mobile menu
    function openMobileMenu() {
      mobileMenu.classList.remove('hidden');
      setTimeout(() => {
        mobileMenuPanel.classList.remove('translate-x-full');
      }, 10);
      document.body.style.overflow = 'hidden'; // Prevent body scroll
    }
    
    // Close mobile menu
    function closeMobileMenu() {
      mobileMenuPanel.classList.add('translate-x-full');
      setTimeout(() => {
        mobileMenu.classList.add('hidden');
      }, 300);
      document.body.style.overflow = ''; // Restore body scroll
    }
    
    // Menu button click
    if (menuBtn) {
      menuBtn.addEventListener('click', function(e) {
        e.preventDefault();
        openMobileMenu();
      });
    }
    
    // Close button click
    if (closeBtn) {
      closeBtn.addEventListener('click', function(e) {
        e.preventDefault();
        closeMobileMenu();
      });
    }
    
    // Backdrop click
    if (backdrop) {
      backdrop.addEventListener('click', function(e) {
        e.preventDefault();
        closeMobileMenu();
      });
    }
    
    // Escape key handler
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && !mobileMenu.classList.contains('hidden')) {
        closeMobileMenu();
      }
    });
    
    // Close menu when clicking on navigation links
    const mobileNavLinks = document.querySelectorAll('.mobile-nav-link');
    mobileNavLinks.forEach(link => {
      link.addEventListener('click', function() {
        closeMobileMenu();
      });
    });
    
    // Set active state for mobile navigation
    const path = window.location.pathname;
    mobileNavLinks.forEach(link => {
      const href = link.getAttribute('href');
      if ((path === '/' && href === '/') || href === path || href === path + '/') {
        link.classList.add('text-primary', 'dark:text-primary');
      }
    });
  })();
</script>
